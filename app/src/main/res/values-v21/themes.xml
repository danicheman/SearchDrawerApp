<?xml version="1.0" encoding="utf-8"?>
<resources>
    <style name="Theme.mySearchViewTheme" parent="@style/Theme.AppCompat.NoActionBar">
        <item name="searchViewStyle">@style/MySearchView</item>
        <item name="android:textColorPrimary">@android:color/holo_green_dark</item> <!-- search text color -->
    </style>

    <style name="MySearchView" parent="Widget.AppCompat.SearchView">
        <item name="voiceIcon">@drawable/ic_record_voice_over</item> <!--doesn't work-->
        <item name="queryBackground">@android:color/black</item><!-- search box background color -->
    </style>
</resources>